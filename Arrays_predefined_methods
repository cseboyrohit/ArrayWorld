import java.util.*;
public class explainArray {

    public static void main(String[] args) {
        //charAt(int index): Returns the character at the specified index.

//      String name = "Hello";
//      for(int i=0; i<name.length(); i++){
//          System.out.println(name.charAt(i));
//      }

            // String Working

//        char data[] = {'a', 'b', 'c'};
//        String str = new String(data);
//        System.out.println(str);  // abc

        // there are various predefined methods

      /* 1. Arrays.toString(): This method converts an array into a string representation.
                It's useful for debugging purposes or for printing arrays.

                java:-

                int[] numbers = {1, 2, 3, 4, 5};
                System.out.println(Arrays.toString(numbers));

       */
        /*Arrays.copyOf(): This method creates a copy of an array with a specified length.
           It's useful when you need to resize an array.

           Java:-

           int[] numbers = {1, 2, 3, 4, 5};
            int[] copy = Arrays.copyOf(numbers, numbers.length);

         */
         /*3.  Arrays.sort(): This method sorts an array in ascending order.

                Java:-

                int[] numbers = {5, 2, 8, 1, 3};
                Arrays.sort(numbers);

          */
        /*
        Arrays.binarySearch(): This method searches for a specific element in a sorted array
         using the binary search algorithm.

         Java;-

         int[] numbers = {1, 2, 3, 4, 5};
         int index = Arrays.binarySearch(numbers, 3);

         */
        /*
           Arrays.equals(): This method checks if two arrays are equal or not.

           Java:-

           int[] array1 = {1, 2, 3};
           int[] array2 = {1, 2, 3};
           boolean isEqual = Arrays.equals(array1, array2);

         */
        /*
          System.arraycopy() method copies an array from the specified source

         public static void arraycopy(Object src, int srcPos, Object dest, int destPos, int length)

         int[] srcArray = {1, 2, 3, 4, 5};
         int[] destArray = new int[5];
         System.arraycopy(srcArray, 0, destArray, 0, 5);


   *  src: The source array from which elements will be copied.
   *  srcPos: The starting position in the source array from which to start copying.
   *  dest: The destination array where elements will be copied.
*  *  destPos: The starting position in the destination array where elements will be copied.
*  *  length: The number of elements to be copied.

    }
}

